import 'package:flutter/material.dart';

/// Spiritual, calm, high-contrast palette for Labbaik.
/// Gold = kiswa accent, green = Islamic, cream = warm background.
class AppColors {
  AppColors._();

  static const primary = Color(0xFF0E5A43); // deep emerald green
  static const primaryDark = Color(0xFF0A4634);
  static const accentGold = Color(0xFFC9A24A); // kiswa gold
  static const background = Color(0xFFFAF7F0); // warm cream
  static const surface = Color(0xFFFFFFFF);
  static const kaabaBlack = Color(0xFF14110E); // story headers / night moments
  static const textPrimary = Color(0xFF1C1C1C);
  static const textSecondary = Color(0xFF5A5A5A);
  static const success = Color(0xFF2E7D32);
  static const locked = Color(0xFFBDBDBD);
  static const divider = Color(0xFFE6E0D4);
}
